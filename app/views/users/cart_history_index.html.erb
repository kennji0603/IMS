<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <span>
        <%= link_to "マイページ", user_path(current_user.id) %> > 注文履歴
      </span>

      <div class="container mt-4">
        <table class="table">
          <thead>
            <tr>
              <th scope="col">注文番号</th>
              <th scope="col">購入日時</th>
              <th scope="col">合計金額</th>
              <th scope="col">詳細</th>
            </tr>
          </thead>
          <tbody>
            <% @orders.each do |order| %>
            <tr>
              <td><%= order.id %></td>
              <td><%= order.updated_at.to_datetime.strftime("%Y-%m-%d %H:%M:%S") %></td>
              <td><%= order.total.fractional / 100 %></td>
              <td>
                <%= link_to "#{ order.id }", cart_histpry_show_users_path(order) %>
              </td>
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>

      
    </div>
  </div>
</div>