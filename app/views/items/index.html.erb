<%= render "shared/header" %>
<div class="row">
  <div class="col-9">
    <div class="container mt-4">
      <div class="row w-100">
        
        <% @items.each do |item|%>
        <div class="col-3" >
          <%= link_to item_path(item) do %>
            <%= image_tag item.image, class: "img-thumbnail" %>
          <% end %>
          <div class="row">
            <div class="col-12">
              <p class="ics-item-label mt-2">
                <%= item.name %><br>
                <label>¥<%= item.price %></label>
              </p>
          
            </div>
          </div>
        </div>
        <% end %>

      </div>
    </div>
  </div>
</div>
<%= render "shared/footer"%>